# CaneKit — XcodeGen project spec.
# Regenerate with `scripts/gen.sh` (never edit CaneKit.xcodeproj by hand; it is git-ignored).
#
# Targets
#   CaneKit        iPhone app (iOS 26+)          bundle id com.aritro.canekit
#   CaneKitWatch   watchOS companion (26+)       bundle id com.aritro.canekit.watchkitapp
#   CaneKitWidget  Live Activity extension       bundle id com.aritro.canekit.widget   [added in step 9]
# All three share the pure-logic SwiftPM package in ./Logic.
#
# Signing: automatic, personal team. DEVELOPMENT_TEAM is passed on the xcodebuild command line
# (see Makefile / local.mk) so this file never carries a team id.
#
# Concurrency: Swift 6 language mode with Xcode 26 "approachable concurrency" defaults
# (everything is MainActor unless it opts out with `nonisolated`). Only the ARKit frame
# processor and the network clients opt out.

name: CaneKit

options:
  bundleIdPrefix: com.aritro
  deploymentTarget:
    iOS: "26.0"
    watchOS: "26.0"
  createIntermediateGroups: true
  generateEmptyDirectories: true
  xcodeVersion: "26.0"

packages:
  CaneKitLogic:
    path: Logic

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete
    SWIFT_DEFAULT_ACTOR_ISOLATION: MainActor
    SWIFT_APPROACHABLE_CONCURRENCY: "YES"
    SWIFT_UPCOMING_FEATURE_NONISOLATED_NONSENDING_BY_DEFAULT: "YES"
    CODE_SIGN_STYLE: Automatic
    ENABLE_USER_SCRIPT_SANDBOXING: "YES"
    SWIFT_TREAT_WARNINGS_AS_ERRORS: "NO"
    IPHONEOS_DEPLOYMENT_TARGET: "26.0"
    WATCHOS_DEPLOYMENT_TARGET: "26.0"
    CURRENT_PROJECT_VERSION: "1"
    MARKETING_VERSION: "0.1"

targets:

  # ───────────────────────────── iPhone app ─────────────────────────────
  CaneKit:
    type: application
    platform: iOS
    sources:
      - path: CaneKit
        excludes:
          - "Resources/Secrets.plist"     # copied in as a build resource below so a missing file is not fatal
      - path: CaneKit/Resources/Secrets.plist
        optional: true
        buildPhase: resources
      - path: Shared/LiveActivity
    dependencies:
      - package: CaneKitLogic
      - target: CaneKitWatch
        embed: true
      - target: CaneKitWidget
        embed: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.aritro.canekit
        PRODUCT_NAME: CaneKit
        TARGETED_DEVICE_FAMILY: "1"
        INFOPLIST_KEY_UIUserInterfaceStyle: Automatic
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        SUPPORTS_MACCATALYST: "NO"
        CODE_SIGN_ENTITLEMENTS: CaneKit/CaneKit.entitlements
    info:
      path: CaneKit/Info.plist
      properties:
        CFBundleDisplayName: CaneKit
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        UILaunchScreen: {}
        UISupportedInterfaceOrientations: [UIInterfaceOrientationPortrait]
        UIRequiredDeviceCapabilities: [arkit, arm64]
        UIBackgroundModes: [audio, location]
        NSSupportsLiveActivities: true
        ITSAppUsesNonExemptEncryption: false
        # Purpose strings — the system shows these on first use.
        NSCameraUsageDescription: "CaneKit uses the camera and LiDAR to detect obstacles between your waist and head."
        NSLocationWhenInUseUsageDescription: "CaneKit uses your location and heading to guide you along the route."
        NSMotionUsageDescription: "CaneKit uses motion to tell when the cane is sweeping, to track your head direction with AirPods, and to count steps."
        NSSpeechRecognitionUsageDescription: "CaneKit can take spoken commands."
        NSMicrophoneUsageDescription: "CaneKit uses the microphone for spoken commands."
        NSHealthShareUsageDescription: "CaneKit reads your step count for the arrival summary."
        NSHealthUpdateUsageDescription: "CaneKit records the guided walk as a workout so the watch can keep giving cues."

  # ───────────────────────────── Watch app ─────────────────────────────
  CaneKitWatch:
    type: application
    platform: watchOS
    sources:
      - path: CaneKitWatch
    dependencies:
      - package: CaneKitLogic
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.aritro.canekit.watchkitapp
        PRODUCT_NAME: CaneKit
        TARGETED_DEVICE_FAMILY: "4"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        CODE_SIGN_ENTITLEMENTS: CaneKitWatch/CaneKitWatch.entitlements
    info:
      path: CaneKitWatch/Info.plist
      properties:
        CFBundleDisplayName: CaneKit
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        WKApplication: true
        WKCompanionAppBundleIdentifier: com.aritro.canekit
        WKRunsIndependentlyOfCompanionApp: false
        WKBackgroundModes: [workout-processing, mindfulness]   # workout keep-alive + runtime-session fallback
        NSMotionUsageDescription: "CaneKit counts steps for the arrival summary."
        NSHealthShareUsageDescription: "CaneKit reads your step count for the arrival summary."
        NSHealthUpdateUsageDescription: "CaneKit records the guided walk as a workout so wrist cues keep working."

  # ───────────────────────────── Live Activity widget ─────────────────────────────
  CaneKitWidget:
    type: app-extension
    platform: iOS
    sources:
      - path: CaneKitWidget
      - path: Shared/LiveActivity
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.aritro.canekit.widget
        PRODUCT_NAME: CaneKitWidget
        TARGETED_DEVICE_FAMILY: "1"
        SKIP_INSTALL: "YES"
    info:
      path: CaneKitWidget/Info.plist
      properties:
        CFBundleDisplayName: CaneKit
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension

schemes:
  CaneKit:
    build:
      targets:
        CaneKit: all
    run:
      config: Debug
    archive:
      config: Release
  CaneKitWatch:
    build:
      targets:
        CaneKitWatch: all
    run:
      config: Debug
